/* ============================================================
   Dev Luminary — Supabase configuration
   ------------------------------------------------------------
   PASTE YOUR CREDENTIALS BELOW. That's the only edit needed.

   Where to find them:
     Supabase Dashboard → your project → Project Settings → API
       - "Project URL"        → SUPABASE_URL
       - "anon" / "public" key → SUPABASE_ANON_KEY

   ⚠️ Only ever put the "anon" / "publishable" key here.
   ⚠️ NEVER put your "service_role" key, database password, or any
      other secret key in this file — this file is public, it ships
      to every visitor's browser exactly like the rest of the site.
   ============================================================ */

window.SUPABASE_URL = "https://cnekkazllvqzrqbyppjs.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable_9qZ2eYo81JzV1XIzacKRSQ__1OMiOLX";
